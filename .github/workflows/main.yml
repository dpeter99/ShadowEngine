name: CI

on: 
  push:
    branches: 
      - master

jobs:
  build:
    runs-on: windows-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: Setup Nuget.exe
      uses: warrenbuckley/Setup-Nuget@v1
    - name: restore nuget
      run: nuget restore $$GITHUB_WORKSPACE$$/ShadowEngine.sln
    - name: Build Docs
      run: docs-source/build-docs.cmd
