<UserControl x:Class="ShadowLight.Pages.Controlls.AssetBrowserView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ShadowLight.Pages.Controlls"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance local:AssetBrowserViewModel}"
             d:DesignHeight="450" d:DesignWidth="200">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="20"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Label Background="#FFD4A35C" />
        <TreeView ItemsSource="{Binding}"> 
            
        </TreeView>
    </Grid>
</UserControl>
